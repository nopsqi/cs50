CREATE TABLE students (
    id INTEGER NOT NULL,
    student_name TEXT NOT NULL
    PRIMARY KEY(id)
);
CREATE TABLE houses (
    id INTEGER NOT NULL,
    house TEXT NOT NULL,
    head, TEXT NOT NULL,
    PRIMARY KEY(id)
);
CREATE TABLE assignment (
    id INTEGER NOT NULL,
    student_id 
)